<role>
Actúa como un Arquitecto de Bases de Datos Senior y experto en Ingeniería de Prompts.
Tu objetivo es generar un script SQL de alta calidad para crear la tabla Cliente 
y asegurar su persistencia en el sistema de archivos del usuario.
</role>

<context>
- Motor de DB: Microsoft SQL Server 2022 Express (Optimizar considerando límite de 10GB).
- Base de Datos: "dcmo_utm_dacb" (La base de datos ya existe).
- Esquema: [dbo]
- Ubicación de Salida: "/db/scripts/03_create_clientes_structure.sql".
</context>

<task>
1. Generar un script DDL completo para crear la tabla [dbo].[Cliente].
2. Definir tipos de datos optimizados usando NVARCHAR para soporte Unicode.
3. Establecer la Primary Key como INT IDENTITY(1,1) PRIMARY KEY CLUSTERED.
4. Implementar restricciones de integridad con nombres explícitos.
5. Agregar validaciones CHECK para garantizar calidad de datos.
6. Garantizar que el script sea completamente idempotente.
</task>

<requirements>
Crear la tabla [dbo].[Cliente] con las siguientes columnas:

- ClienteID INT IDENTITY(1,1) NOT NULL PRIMARY KEY CLUSTERED
- Nombre NVARCHAR(100) NOT NULL
- ApellidoPaterno NVARCHAR(100) NOT NULL
- ApellidoMaterno NVARCHAR(100) NULL
- Telefono VARCHAR(15) NULL
- Email NVARCHAR(150) NOT NULL
- Direccion NVARCHAR(200) NULL
- FechaRegistro DATETIME NOT NULL DEFAULT GETDATE()
- Activo BIT NOT NULL DEFAULT 1

Restricciones obligatorias:

- CONSTRAINT UQ_Cliente_Email UNIQUE (Email)
- CONSTRAINT CK_Cliente_Activo CHECK (Activo IN (0,1))
- CONSTRAINT CK_Cliente_Email_Formato CHECK (Email LIKE '%_@_%._%')
</requirements>

<sql_best_practices>
- Incluir USE dcmo_utm_dacb;
- Incluir SET NOCOUNT ON;
- Incluir SET XACT_ABORT ON;
- Verificar existencia usando IF NOT EXISTS (SELECT 1 FROM sys.tables WHERE name = 'Cliente').
- Envolver en bloque BEGIN TRY / BEGIN CATCH con control transaccional.
- Documentar cada sección con comentarios técnicos.
</sql_best_practices>

<file_system_instructions>
- Verificar si existe el directorio "/db/scripts/".
- Si no existe, crearlo.
- Guardar el archivo en la ruta especificada.
- Confirmar la operación después de guardar.
</file_system_instructions>