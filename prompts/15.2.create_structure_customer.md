<role>
Actúa como un Arquitecto Senior de Bases de Datos.
Genera un script SQL profesional para registrar exactamente 20 clientes
en la tabla [dbo].[Cliente], asegurando integridad de datos y evitando duplicados.
</role>

<context>
- Motor de DB: Microsoft SQL Server 2022 Express.
- Base de Datos: "dcmo_utm_dacb".
- La tabla [dbo].[Cliente] ya existe.
- Ubicación de Salida: "/db/scripts/04_insert_clientes_data.sql".
</context>

<task>
1. Generar un script SQL para insertar exactamente 20 clientes.
2. Evitar insertar clientes duplicados usando Email como llave natural.
3. Validar formato básico del Email antes del INSERT.
4. Permitir que el script pueda ejecutarse múltiples veces sin generar duplicados.
5. Implementar control transaccional y manejo de errores.
</task>

<requirements>
- Incluir:
    USE dcmo_utm_dacb;
    GO
    SET NOCOUNT ON;
    SET XACT_ABORT ON;

- Envolver todo en:
    BEGIN TRY
        BEGIN TRANSACTION
        ...
        COMMIT
    END TRY
    BEGIN CATCH
        ROLLBACK
        THROW;
    END CATCH

- Antes de cada INSERT utilizar:

IF NOT EXISTS (
    SELECT 1
    FROM dbo.Cliente
    WHERE LOWER(Email) = LOWER('correo@ejemplo.com')
)
AND 'correo@ejemplo.com' LIKE '%_@_%._%'

- Generar exactamente 20 clientes con datos realistas mexicanos.
- Cada cliente debe tener Email único.
- No modificar valores IDENTITY manualmente.
- No eliminar datos existentes.
</requirements>

<sql_best_practices>
- Script completamente idempotente.
- Mantener consistencia en nomenclatura.
- Documentar cada sección con comentarios técnicos.
</sql_best_practices>

<file_system_instructions>
- Verificar si existe el directorio "/db/scripts/".
- Si no existe, crearlo.
- Guardar el archivo como:
    "/db/scripts/04_insert_clientes_data.sql"
- Confirmar la operación después de guardar.
</file_system_instructions>